# CommitGrep Examples

Run `node dist/cli.js <repo> ...` from the project root after `pnpm run build`.

1. `find-tickets.sh` — list commits referencing ticket IDs touching a subsystem.
2. `churn-by-author.sh` — aggregate insertions per author for a month window.
3. `report-example.json` — sample JSON report output.
